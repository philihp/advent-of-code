import {} from './part2.js'
import { describe, it } from 'node:test'
import assert from 'node:assert/strict'

describe('applyButton', () => {
  it('compiles', () => {
    const actual = undefined
    assert.equals(actual, undefined)
  })
})
